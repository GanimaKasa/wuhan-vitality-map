fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
pydantic>=2.0
requests>=2.31
python-dotenv>=1.0
numpy>=1.24
# 注意：这里没有sentence-transformers/torch —— 微博语义检索的embedding（包括离线批量
# 计算 backend/weibo_embed.py 和在线查询 backend/app.py）都改成调用SiliconFlow云端
# embeddings API，不需要本地跑模型，部署环境(Render免费档512MB)也能轻松装下。
